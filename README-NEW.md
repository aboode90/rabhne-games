# 🎮 Rabhne Games - النظام المحسن v2.0

منصة ألعاب HTML5 عربية احترافية مع نظام نقاط متقدم وحماية شاملة من التلاعب.

## ✨ المميزات الجديدة

### 🔒 نظام الأمان المتقدم
- **حماية شاملة من التلاعب**: نظام SecurityManager متقدم
- **مراقبة معدل الطلبات**: حماية من الهجمات والاستخدام المفرط
- **تشفير البيانات الحساسة**: حماية معلومات المستخدمين
- **مراقبة أدوات المطور**: كشف محاولات التلاعب
- **Content Security Policy**: حماية من هجمات XSS

### 🎨 واجهة المستخدم المحسنة
- **تصميم احترافي حديث**: واجهة مستخدم متقدمة مع تأثيرات CSS3
- **نظام إشعارات ذكي**: Toast notifications مع تأثيرات متقدمة
- **تجربة مستخدم سلسة**: انتقالات وتأثيرات محسنة
- **تصميم متجاوب**: يعمل بشكل مثالي على جميع الأجهزة
- **وضع الظلام**: دعم للثيمات المتعددة

### ⚡ الأداء والسرعة
- **تحميل سريع**: تحسينات الأداء وتقليل وقت التحميل
- **Service Worker محسن**: تخزين مؤقت ذكي وعمل بدون إنترنت
- **PWA متقدم**: تطبيق ويب تقدمي بميزات متقدمة
- **تحسين الصور**: ضغط وتحسين الصور تلقائياً
- **Lazy Loading**: تحميل المحتوى عند الحاجة

### 🎯 نظام النقاط المحسن
- **حماية من التلاعب**: نظام PointsManager آمن ومحسن
- **معاملات آمنة**: تسجيل جميع المعاملات مع التحقق
- **حدود ذكية**: نظام حدود يومية وفترات انتظار محسن
- **إحصائيات متقدمة**: تتبع شامل لنشاط المستخدمين
- **مزامنة في الوقت الفعلي**: تحديث فوري للنقاط والبيانات

## 🏗️ الهيكل الجديد

```
├── config/                 # إعدادات التطبيق
│   └── app-config.js       # الإعدادات الرئيسية
├── core/                   # النواة الأساسية
│   ├── app-core.js         # نواة التطبيق الرئيسية
│   ├── auth/               # نظام التوثيق
│   │   └── auth-manager.js # مدير التوثيق المحسن
│   ├── security/           # نظام الأمان
│   │   └── security-manager.js # مدير الأمان المتقدم
│   ├── utils/              # الأدوات المساعدة
│   │   └── ui-manager.js   # مدير واجهة المستخدم
│   └── api/                # واجهات برمجة التطبيقات
│       └── points-manager.js # مدير النقاط المحسن
├── css/
│   └── style-new.css       # التصميم المحسن
├── index-new.html          # الصفحة الرئيسية المحسنة
├── sw-new.js              # Service Worker محسن
├── manifest-new.json      # PWA Manifest محسن
└── README-NEW.md          # هذا الملف
```

## 🚀 التثبيت والإعداد

### المتطلبات
- خادم ويب (Apache/Nginx/Node.js)
- حساب Firebase مع Firestore
- شهادة SSL (مطلوبة للـ PWA)

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone https://github.com/your-repo/rabhne-games.git
cd rabhne-games
```

2. **إعداد Firebase**
- إنشاء مشروع Firebase جديد
- تفعيل Authentication (Google)
- تفعيل Firestore Database
- نسخ إعدادات Firebase إلى `config/app-config.js`

3. **إعداد قواعد Firestore**
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // قواعد المستخدمين
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      allow read: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }
    
    // قواعد الألعاب
    match /games/{gameId} {
      allow read: if true;
      allow write: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }
    
    // قواعد المعاملات
    match /transactions/{transactionId} {
      allow read: if request.auth != null && resource.data.userId == request.auth.uid;
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
    }
    
    // قواعد طلبات السحب
    match /withdraw_requests/{requestId} {
      allow read: if request.auth != null && 
        (resource.data.userId == request.auth.uid || 
         get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true);
      allow create: if request.auth != null && request.resource.data.userId == request.auth.uid;
      allow update: if request.auth != null && 
        get(/databases/$(database)/documents/users/$(request.auth.uid)).data.isAdmin == true;
    }
  }
}
```

4. **رفع الملفات**
- رفع جميع الملفات إلى الخادم
- التأكد من وجود شهادة SSL
- إعداد إعادة التوجيه للـ PWA

5. **اختبار النظام**
- فتح الموقع في المتصفح
- اختبار تسجيل الدخول
- اختبار نظام النقاط
- اختبار الـ PWA

## 🔧 الإعدادات

### إعدادات التطبيق الأساسية
```javascript
const APP_CONFIG = {
    POINTS: {
        PER_CLAIM: 1,           // النقاط لكل مطالبة
        COOLDOWN_SECONDS: 30,   // فترة الانتظار بالثواني
        DAILY_LIMIT: 2880,      // الحد الأقصى اليومي
        MIN_WITHDRAW: 2000,     // الحد الأدنى للسحب
        TO_DOLLAR_RATE: 10000   // معدل تحويل النقاط للدولار
    },
    SECURITY: {
        SESSION_TIMEOUT: 3600000,        // انتهاء الجلسة
        MAX_DAILY_SESSIONS: 100,         // الحد الأقصى للجلسات اليومية
        MAX_LOGIN_ATTEMPTS: 5,           // محاولات تسجيل الدخول
        RATE_LIMIT_WINDOW: 60000,        // نافزة معدل الطلبات
        MAX_REQUESTS_PER_MINUTE: 60      // الحد الأقصى للطلبات بالدقيقة
    }
};
```

### إعدادات الأمان
- تفعيل HTTPS إجباري
- إعداد Content Security Policy
- تكوين معدل الطلبات
- مراقبة محاولات التلاعب

## 📱 ميزات PWA

### التثبيت
- إمكانية تثبيت التطبيق على الجهاز
- أيقونات مخصصة لجميع الأحجام
- شاشة البداية المخصصة

### العمل بدون إنترنت
- تخزين مؤقت ذكي للملفات الأساسية
- عمل محدود بدون إنترنت
- مزامنة تلقائية عند عودة الاتصال

### الإشعارات
- إشعارات push للأحداث المهمة
- تنبيهات النقاط والمكافآت
- إشعارات حالة السحب

## 🛡️ الأمان والحماية

### حماية من التلاعب
- مراقبة أدوات المطور
- تشفير البيانات الحساسة
- التحقق من صحة جميع المدخلات
- مراقبة معدل الطلبات

### حماية البيانات
- تشفير البيانات الحساسة
- حماية من هجمات XSS و CSRF
- تسجيل جميع الأنشطة المشبوهة
- نسخ احتياطية تلقائية

### مراقبة النظام
- تسجيل جميع الأحداث الأمنية
- مراقبة الأداء والأخطاء
- تنبيهات الأنشطة المشبوهة
- تقارير أمنية دورية

## 📊 التحليلات والإحصائيات

### إحصائيات المستخدمين
- عدد المستخدمين النشطين
- معدل النشاط اليومي
- إحصائيات اللعب والنقاط
- تحليل سلوك المستخدمين

### إحصائيات الأداء
- أوقات التحميل
- معدل الأخطاء
- استخدام الموارد
- إحصائيات الشبكة

### تقارير مالية
- إجمالي النقاط الممنوحة
- طلبات السحب والمدفوعات
- الإيرادات والمصروفات
- تحليل الربحية

## 🔄 التحديثات والصيانة

### التحديثات التلقائية
- تحديث Service Worker تلقائياً
- تحديث البيانات في الوقت الفعلي
- إشعارات التحديثات الجديدة
- نسخ احتياطية قبل التحديث

### الصيانة الدورية
- تنظيف قاعدة البيانات
- تحسين الأداء
- تحديث الأمان
- مراجعة السجلات

## 🐛 استكشاف الأخطاء

### الأخطاء الشائعة
1. **خطأ Firebase**: التحقق من إعدادات Firebase
2. **خطأ PWA**: التأكد من وجود HTTPS
3. **خطأ النقاط**: فحص قواعد Firestore
4. **خطأ التوثيق**: التحقق من إعدادات Google OAuth

### أدوات التشخيص
- وحدة تحكم المطور
- Firebase Console
- تقارير الأخطاء التلقائية
- سجلات النظام

## 📞 الدعم والمساعدة

### التواصل
- البريد الإلكتروني: support@rabhne.online
- الموقع الرسمي: www.rabhne.online
- التوثيق: docs.rabhne.online

### المساهمة
- تقديم تقارير الأخطاء
- اقتراح ميزات جديدة
- المساهمة في الكود
- تحسين التوثيق

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT. راجع ملف [LICENSE](LICENSE) للمزيد من التفاصيل.

## 🙏 شكر وتقدير

شكر خاص لجميع المساهمين والمطورين الذين ساعدوا في تطوير هذا المشروع.

---

**تم تطوير هذا النظام بـ ❤️ في المملكة العربية السعودية**

© 2024 Rabhne Games. جميع الحقوق محفوظة.